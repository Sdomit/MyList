<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:converters="clr-namespace:MyList.Converters"
                    xmlns:helpers="clr-namespace:MyList.Helpers">
    <converters:MultiplyValueConverter x:Key="MultiplyValueConverter" />

    <Style TargetType="Window">
        <Setter Property="Background" Value="{DynamicResource Token.BgMicaBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource Token.TextPrimaryBrush}" />
        <Setter Property="FontFamily" Value="{DynamicResource Token.FontUi}" />
        <Setter Property="FontSize" Value="{DynamicResource Token.TextMd}" />
        <Setter Property="Icon" Value="pack://siteoforigin:,,,/icon.ico" />
        <Setter Property="UseLayoutRounding" Value="True" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <!-- Display-mode formatting pixel-snaps glyphs: crisper small UI text than the
             default Ideal mode. Inherits to every TextBlock in the window. -->
        <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
    </Style>

    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource Token.TextPrimaryBrush}" />
    </Style>

    <Style TargetType="Button" BasedOn="{StaticResource Simple.Button}" />

    <Style TargetType="TextBox" BasedOn="{StaticResource Simple.TextBox}" />

    <Style TargetType="ListBox">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="ScrollViewer.CanContentScroll" Value="True" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True" />
        <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling" />
        <Setter Property="VirtualizingPanel.ScrollUnit" Value="Pixel" />
    </Style>

    <!-- Implicit ListBoxItem stays distinct from Simple.ItemListBoxItem:
         Simple.ItemListBoxItem is row-shaped with health/accent triggers (used by the
         main items ListBox); this implicit style covers Collections sidebar, Pinned /
         Recent / Trending sidebar lists, ContextMenus, and dialog pickers. -->
    <Style TargetType="ListBoxItem">
        <Setter Property="Padding" Value="{DynamicResource DensityListItemPadding}" />
        <Setter Property="Margin" Value="0,2" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="RenderTransform">
            <Setter.Value>
                <TranslateTransform Y="0" />
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="{DynamicResource Token.Skin.RowRadius}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource ItemHover}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{DynamicResource ItemSelected}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                         From="0"
                                         To="1"
                                         Duration="0:0:0.18" />
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
