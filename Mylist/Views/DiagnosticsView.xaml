<UserControl x:Class="MyList.Views.DiagnosticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{DynamicResource SurfaceBackground}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="{StaticResource Token.RadiusXl}"
            Padding="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Diagnostics" FontSize="16" FontWeight="SemiBold" />
                <Button Content="Refresh"
                        Margin="12,0,0,0"
                        Padding="10,4"
                        Command="{Binding RefreshCommand}" />
            </StackPanel>

            <StackPanel Grid.Row="1" Margin="0,10,0,10">
                <TextBlock Text="{Binding HotkeyStatus}" Margin="0,0,0,2" />
                <TextBlock Text="{Binding EffectiveHotkey}" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,8" />
                <TextBlock Text="{Binding TrayStatus}" Margin="0,0,0,4" />
                <TextBlock Text="{Binding NetworkStatus}" Margin="0,0,0,4" />
                <TextBlock Text="{Binding ExplorerIntegrationStatus}" Margin="0,0,0,4" />
                <TextBlock Text="{Binding DebugStatus}" Foreground="{DynamicResource TextSecondary}" TextWrapping="Wrap" />
            </StackPanel>

            <ListBox Grid.Row="2"
                     ItemsSource="{Binding LogTail}" />
        </Grid>
    </Border>
</UserControl>
