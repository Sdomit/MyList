<Window x:Class="MyList.Views.MtabPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Mtab"
        Width="620"
        Height="520"
        MinWidth="540"
        MinHeight="420"
        Background="{DynamicResource AppBackground}"
        Foreground="{DynamicResource TextPrimary}"
        WindowStartupLocation="CenterOwner">
    <Border Margin="12"
            Background="{DynamicResource SurfaceBackground}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="12"
            Padding="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="Add To Existing Mtab"
                       FontSize="18"
                       FontWeight="SemiBold" />

            <StackPanel Grid.Row="1"
                        Margin="0,10,0,12">
                <TextBlock x:Name="IncomingSummaryText"
                           Foreground="{DynamicResource TextSecondary}"
                           Margin="0,0,0,8" />
                <TextBox MinHeight="32"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         ToolTip="Search Mtab by name or path"
                         Margin="0" />
            </StackPanel>

            <Grid Grid.Row="2">
                <Border Background="{DynamicResource SurfaceElevated}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="10"
                        Padding="10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <TextBlock Text="{Binding Mtabs.Count, StringFormat=Available Mtabs: {0}}"
                                   Foreground="{DynamicResource TextSecondary}"
                                   Margin="0,0,0,8" />

                        <ListBox x:Name="MtabListBox"
                                 Grid.Row="1"
                                 ItemsSource="{Binding FilteredMtabs}"
                                 SelectedItem="{Binding SelectedMtab}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Padding="8"
                                            Margin="0,0,0,6"
                                            Background="{DynamicResource SurfaceBackground}"
                                            BorderBrush="{DynamicResource BorderBrush}"
                                            BorderThickness="1"
                                            CornerRadius="8">
                                        <StackPanel>
                                            <TextBlock Text="{Binding Name}"
                                                       FontWeight="SemiBold"
                                                       TextTrimming="CharacterEllipsis" />
                                            <TextBlock Text="{Binding DisplayPath}"
                                                       Foreground="{DynamicResource TextSecondary}"
                                                       Margin="0,2,0,0" />
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>

                <TextBlock Text="No existing Mtab found."
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource TextSecondary}">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasMtabs}" Value="False">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>

            <StackPanel Grid.Row="3"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,12,0,0">
                <Button Content="Cancel"
                        Width="100"
                        Margin="0,0,8,0"
                        Click="OnCancel" />
                <Button Content="Add to Mtab"
                        Width="132"
                        Style="{StaticResource PrimaryButton}"
                        Click="OnConfirm" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
