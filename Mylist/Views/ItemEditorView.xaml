<UserControl x:Class="MyList.Views.ItemEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{DynamicResource SurfaceBackground}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="12"
            Padding="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel>
                <TextBlock Text="Item Editor" FontSize="16" FontWeight="SemiBold" />
                <TextBlock Text="Edit name, path, and tags" Foreground="{DynamicResource TextSecondary}" FontSize="11" />
            </StackPanel>

            <ScrollViewer Grid.Row="1" Margin="0,12,0,12" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Name" Margin="0,0,0,6" />
                    <TextBox Text="{Binding Name}" />

                    <TextBlock Text="Path" Margin="0,12,0,6" />
                    <TextBox Text="{Binding Path}" />

                    <TextBlock Text="Type" Margin="0,12,0,6" />
                    <ComboBox ItemsSource="{Binding ItemTypes}" SelectedItem="{Binding Type}" />

                    <TextBlock Text="Icon override path" Margin="0,12,0,6" />
                    <TextBox Text="{Binding IconPath}" />

                    <CheckBox Content="Use system icon" Margin="0,10,0,0" IsChecked="{Binding UseSystemIcon}" />
                    <CheckBox Content="Favorite" Margin="0,6,0,0" IsChecked="{Binding IsFavorite}" />

                    <TextBlock Text="Tags (comma separated)" Margin="0,12,0,6" />
                    <TextBox Text="{Binding Tags}" />

                    <TextBlock Text="Launch arguments" Margin="0,12,0,6" />
                    <TextBox Text="{Binding LaunchArguments}" />

                    <TextBlock Text="Working directory" Margin="0,12,0,6" />
                    <TextBox Text="{Binding WorkingDirectory}" />

                    <CheckBox Content="Run as administrator"
                              Margin="0,10,0,0"
                              IsChecked="{Binding RunAsAdmin}" />

                    <TextBlock Text="Terminal profile" Margin="0,12,0,6" />
                    <ComboBox ItemsSource="{Binding TerminalProfiles}"
                              SelectedItem="{Binding TerminalProfile}" />

                    <Border Margin="0,16,0,0" Background="{DynamicResource SurfaceElevated}" CornerRadius="8" Padding="10">
                        <StackPanel>
                            <TextBlock Text="Status" FontWeight="SemiBold" />
                            <TextBlock FontSize="11">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="Available" />
                                        <Setter Property="Foreground" Value="{DynamicResource TextSecondary}" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Item.IsOffline}" Value="True">
                                                <Setter Property="Text" Value="Offline" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Width="90" Margin="0,0,8,0" Command="{Binding CancelCommand}" />
                <Button Content="Save" Width="90" Style="{StaticResource PrimaryButton}" Command="{Binding SaveCommand}" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
